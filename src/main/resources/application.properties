spring.h2.console.enabled=true

# ===============================
# = DATA SOURCE
# ===============================
# Set here configurations for the database connection
# Connection url for the database "netgloo_blog"
# spring.datasource.url = jdbc:mysql://localhost:3306/mmhinz?useTimezone=true&serverTimezone=UTC
# Username and password
#spring.datasource.username = mmhinz
#spring.datasource.password = u4k8y9h9
# Keep the connection alive if idle for a long time (needed in production)
#spring.datasource.testWhileIdle = true
#spring.datasource.validationQuery = SELECT 1
# ===============================
# = JPA / HIBERNATE
# ===============================
# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).
# Show or not log for each sql query
# spring.jpa.show-sql = true
# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
# spring.jpa.hibernate.ddl-auto = create
# Naming strategy
# spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy
# Allows Hibernate to generate SQL optimized for a particular DBMS
# spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
# Lazyfetching: Captura preguiçosa - O objeto associado ou coleção écapturado de "forma preguiçosa", quando éacessado. Resultada éuma nova requisição para o banco de dados (a não ser que o objeto associado esteja no cache).
# spring.jpa.properties.hibernate.enable_lazy_load_no_trans = true
# MONGODB (MongoProperties)
# spring.data.mongodb.authentication-database= # Authentication database name.
spring.data.mongodb.database=mmhinz-mongodb
# spring.data.mongodb.field-naming-strategy= # Fully qualified name of the FieldNamingStrategy to use.
# spring.data.mongodb.grid-fs-database= # GridFS database name.
spring.data.mongodb.host= localhost
# spring.data.mongodb.password= # Login password of the mongo server. Cannot be set with URI
spring.data.mongodb.port= 27017
# spring.data.mongodb.repositories.type=auto Type of Mongo repositories to enable
# spring.data.mongodb.uri=mongodb://localhost/test # Mongo database URI. Cannot be set with host, port and credentials.
# spring.data.mongodb.username= # Login user of the mongo server. Cannot be set with URI.